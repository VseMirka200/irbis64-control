# ИРБИС64 Контроль

Настольное Windows-приложение для сверки записей ИРБИС64 с перечнями из Excel,
формирования отчётов и безопасной установки служебных меток. Программа умеет
работать напрямую с сервером ИРБИС64 либо с локальной TXT-копией базы.

## Возможности

- сверка по ISBN, названию и автору;
- проверка по реестру иностранных агентов;
- отдельный лист Excel для пограничных совпадений, требующих ручной проверки;
- предварительный просмотр изменений перед прямой записью в ИРБИС;
- контроль версий MFN и пропуск конфликтующих записей;
- rollback-копия исходных записей перед изменением;
- сравнение ранее созданных Excel-отчётов и TXT-копий.

В разделе **Источники → Настройка совпадений** откройте выпадающий список,
отметьте нужные поля и нажмите **Добавить**. Доступны ISBN, название, автор, издательство и год.
Например: **Название + издательство + год**, **ISBN + год**,
**Название + автор + издательство + год**. Вводить названия вручную не нужно.
Список остаётся открытым при выборе нескольких полей; повторный щелчок снимает отметку.
С клавиатуры используйте стрелки и пробел, Enter закрывает список.
Порядок выбора полей не важен; повторное добавление правила отклоняется.
Без ISBN можно использовать название или автора с другими полями.
Например, **Автор + издательство + год** работает без названия и ISBN,
но даёт только возможные совпадения для ручной проверки: у автора могут быть разные книги одного издательства за один год.

Добавленные правила находятся в компактном списке с прокруткой и переносом длинных названий. Чтобы отключить правило,
выделите его и нажмите **Удалить**. Последнее правило удалить нельзя.
Прежние включённые правила переносятся автоматически; изменения сохраняются в существующих настройках.
Правила применяются как к TXT-копиям, так и к серверу ИРБИС.
Достаточно одного правила: включённое «Название + автор» не требует совпадения года,
даже если рядом добавлено «Название + автор + год».
Пары **Название + год** и **Название + издательство** дают только возможные совпадения
для ручной проверки и не устанавливают метки. Если подходят несколько дополнительных правил,
в результате указывается наиболее полное; запись не дублируется.

Для новых правил нужны заполненные столбцы Excel **Издательство** и/или **Год издания**
(также распознаются «Издатель», «Год», «Год выпуска», `Publisher`, `Year`).
В ИРБИС используются подполе C (издательство) и подполе D (год) поля 210.
Название сравнивается целиком с учётом нормализации регистра и пунктуации.
Пустые поля и неоднозначные годы вроде «2023–2024» не подтверждают совпадение;
для пары «издательство + год» оба значения должны относиться к одному повторению поля 210.

Автоматически применяются только подтверждённые совпадения с точностью 100%.
Пограничные результаты попадают в отчёт, но не изменяют базу.

## Запуск из исходников

Требуется Python 3.11 или новее. На Windows достаточно запустить:

```bat
start_irbis64-control.bat
```

Скрипт создаст `.venv`, установит зависимости из `requirements.txt` и запустит
приложение. Ручной вариант:

```powershell
py -3 -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
.\.venv\Scripts\python.exe main.py
```

## Сборка EXE

```bat
build_exe.bat
```

В каталоге `dist` появятся основное приложение и отдельный инструмент подключения
к базе. Для распространения нужны оба EXE-файла.

## Архитектура проекта

Основной код собран в пакете `irbis_control`; файлы в корне оставлены только как
точки запуска и прокси для обратной совместимости.

```text
irbis_control/
├── core/                 модели, нормализация и правила сопоставления
│   └── matcher.py
├── application/          настройки приложения и обновления GitHub
│   ├── settings.py
│   └── updater.py
├── infrastructure/       файловая система и протокол ИРБИС64
│   ├── atomic_io.py
│   └── irbis_bridge.py
├── reporting/            экспорт и сравнение отчётов
│   └── result_diff.py
├── ui/                   окна, фоновые операции и локализация Qt
│   ├── main_window.py
│   ├── db_connector_window.py
│   ├── locale.py
│   └── theme.py
├── paths.py              единое разрешение путей к ресурсам
└── __main__.py           запуск через python -m irbis_control
```

Направление зависимостей: UI использует core, reporting и infrastructure; core
не зависит от UI. Новому коду следует импортировать пакетные модули, например
`irbis_control.core.matcher`. Корневые `matcher.py`, `irbis_bridge.py` и другие
прокси предназначены только для старых интеграций.

## Безопасность записи

Перед изменением живой базы программа:

1. повторно читает найденные MFN;
2. проверяет их версии;
3. показывает оператору предварительный список изменений;
4. сохраняет rollback-файл с исходными полями;
5. записывает и повторно читает изменённые записи.

Настройки, отчёты, снимки и локальные TXT-базы сохраняются атомарно: готовый
временный файл заменяет целевой только после полной записи. Это защищает прежнюю
версию файла от ошибки экспорта или внезапного завершения программы.

Rollback-файлы находятся в подкаталоге `backups` каталога данных приложения.
Путь к конкретной копии показывается в результате операции и записывается в
журнал. Rollback содержит исходные версии и поля MFN в JSON; храните его до
проверки результата операции.

Важно: серия изменений на сервере ИРБИС не является одной транзакцией. Если
соединение оборвалось посередине, часть MFN уже могла измениться. В этом случае
не запускайте операцию повторно вслепую: сохраните сообщение об ошибке, журнал и
rollback-файл, затем проверьте перечисленные MFN.

Пароль ИРБИС сохраняется в локальном конфигурационном файле и восстанавливается при следующем запуске.

## Настройки приложения

Постоянный пункт **Настройки** справа в общем меню открывает настройки внутри основного окна
и выделяется подчёркиванием, как остальные разделы.
Кнопка **О программе** в настройках открывает отдельное окно с описанием возможностей, версией, автором и ссылками на проект,
релизы и обратную связь. Интерфейс всегда использует светлую тему.
В настройках можно включить или отключить rollback-копию перед изменением живой базы и
автоматическую проверку обновлений при запуске. Копирование включено по умолчанию;
при попытке его отключить программа показывает отдельное предупреждение.

## Обновления

Проверка выполняется через последний опубликованный GitHub Release и не блокирует
интерфейс. EXE-сборка предлагает скачать Windows-asset и установить его после
закрытия приложения. Поддерживаются:

- установщик с `Setup` или `Installer` в имени;
- ZIP-пакет Windows с обоими EXE;
- переносимый основной EXE.

Загрузчик принимает только HTTPS-адреса GitHub, проверяет заявленный размер и,
если GitHub возвращает digest, SHA-256. Обновление сначала загружается во временный
`.part`-файл, поэтому незавершённая загрузка не считается готовой.

Workflow `.github/workflows/release.yml` собирает оба EXE при публикации тега вида
`v1.2.3`, создаёт `IRBIS64Control-windows-x64.zip` и прикрепляет его к GitHub
Release. Версия тега должна совпадать с `irbis_control.__version__`.

## Тесты

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
.\.venv\Scripts\python.exe -m compileall -q irbis_control main.py db_connector.py
.\.venv\Scripts\python.exe -m ruff check .
```

Тесты также автоматически запускаются в GitHub Actions на Python 3.11 и 3.13.
